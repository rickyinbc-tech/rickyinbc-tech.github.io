const ANALYTICS_ID = "G-07PQV08YPD"; const ANALYTICS_LOAD_DELAY = 7000; window.dataLayer = window.dataLayer || []; window.gtag = window.gtag || function gtag() { window.dataLayer.push(arguments); }; let analyticsLoaded = false; function loadAnalytics() { if (analyticsLoaded) return; analyticsLoaded = true; const script = document.createElement("script"); script.async = true; script.src = `https://www.googletagmanager.com/gtag/js?id=${ANALYTICS_ID}`; document.head.appendChild(script); window.gtag("js", new Date()); window.gtag("config", ANALYTICS_ID, { send_page_view: true, site_area: "fine_art" }); } function scheduleAnalytics() { const startTimer = () => { window.setTimeout(loadAnalytics, ANALYTICS_LOAD_DELAY); }; if (document.readyState === "complete") { startTimer(); } else { window.addEventListener("load", startTimer, { once: true }); } ["pointerdown", "keydown", "touchstart"].forEach((eventName) => { window.addEventListener(eventName, loadAnalytics, { once: true, passive: true }); }); document.addEventListener("visibilitychange", () => { if (document.visibilityState === "hidden") { loadAnalytics(); } }, { once: true }); window.addEventListener("pagehide", loadAnalytics, { once: true }); } scheduleAnalytics(); const year = document.querySelector("#year"); if (year) { year.textContent = new Date().getFullYear(); } function trackEvent(eventName, params = {}, callback) { if (typeof window.gtag !== "function") { if (callback) callback(); return; } const eventParams = { site_area: "fine_art", ...params }; if (callback) { let callbackFired = false; const runCallback = () => { if (callbackFired) return; callbackFired = true; callback(); }; eventParams.event_callback = runCallback; eventParams.event_timeout = 800; window.gtag("event", eventName, eventParams); window.setTimeout(runCallback, 900); return; } window.gtag("event", eventName, eventParams); } function linkLabel(link) { return (link.textContent || link.getAttribute("aria-label") || "").replace(/\s+/g, " ").trim().slice(0, 90); } function destinationPath(href) { try { const url = new URL(href, window.location.href); return url.origin === window.location.origin ? url.pathname : url.hostname; } catch { return href.slice(0, 90); } } const modal = document.querySelector("#artModal"); const modalImage = document.querySelector("#modalImage"); const modalTitle = document.querySelector("#modalTitle"); const modalMeta = document.querySelector("#modalMeta"); const modalNote = document.querySelector("#modalNote"); const modalPrint = document.querySelector("#modalPrint"); const closeButton = document.querySelector(".modal-close"); let previousFocus = null; function openModal(card) { if (!modal || !modalImage || !closeButton) return; previousFocus = document.activeElement; modalImage.removeAttribute("srcset"); modalImage.removeAttribute("sizes"); modalImage.src = card.dataset.full; modalImage.alt = card.querySelector("img")?.alt || ""; modalTitle.textContent = card.dataset.title || "Artwork"; modalMeta.textContent = card.dataset.meta || ""; modalNote.textContent = card.dataset.note || ""; if (modalPrint) { modalPrint.textContent = card.dataset.print || "Archival pigment print. Edition information available on request."; } modal.classList.add("is-open"); modal.setAttribute("aria-hidden", "false"); document.body.style.overflow = "hidden"; closeButton.focus(); trackEvent("artwork_view", { artwork_title: card.dataset.title || "Artwork", artwork_series: card.dataset.series || card.dataset.meta || "", artwork_asset: destinationPath(card.dataset.full || "") }); } function closeModal() { if (!modal) return; modal.classList.remove("is-open"); modal.setAttribute("aria-hidden", "true"); document.body.style.overflow = ""; if (previousFocus) { previousFocus.focus(); } } document.querySelectorAll(".work-card").forEach((card) => { card.addEventListener("click", () => openModal(card)); }); if (closeButton) { closeButton.addEventListener("click", closeModal); } if (modal) { modal.addEventListener("click", (event) => { if (event.target === modal) { closeModal(); } }); } document.addEventListener("keydown", (event) => { if (event.key === "Escape" && modal?.classList.contains("is-open")) { closeModal(); } }); const filterButtons = document.querySelectorAll("[data-filter]"); const filterItems = document.querySelectorAll("[data-series]"); filterButtons.forEach((button) => { button.addEventListener("click", () => { const filter = button.dataset.filter; filterButtons.forEach((item) => item.classList.remove("is-active")); button.classList.add("is-active"); filterItems.forEach((item) => { const show = filter === "all" || item.dataset.series === filter; item.hidden = !show; }); trackEvent("portfolio_filter", { filter_name: filter || "all" }); }); }); document.addEventListener("click", (event) => { const link = event.target.closest?.("a[href]"); if (!link) return; const href = link.getAttribute("href") || ""; const label = linkLabel(link); const lowerSignal = `${href} ${label}`.toLowerCase(); if (href.startsWith("mailto:")) { trackEvent("contact_click", { contact_method: "email", link_label: label }); return; } if (lowerSignal.includes("print") || lowerSignal.includes("licens") || lowerSignal.includes("exhibition") || lowerSignal.includes("inquir") || href.includes("/contact/")) { trackEvent("inquiry_click", { link_label: label, destination: destinationPath(href) }); return; } if (link.closest(".nav-links") || link.classList.contains("brand")) { trackEvent("navigation_click", { link_label: label, destination: destinationPath(href) }); return; } if (link.classList.contains("button")) { trackEvent("cta_click", { link_label: label, destination: destinationPath(href) }); return; } if (/^https?:\/\ const url = new URL(href, window.location.href); if (url.hostname !== window.location.hostname) { trackEvent("external_link_click", { link_label: label, link_domain: url.hostname }); } } }); const inquiryForm = document.querySelector("[data-inquiry-form]"); if (inquiryForm) { inquiryForm.addEventListener("submit", (event) => { event.preventDefault(); const data = new FormData(inquiryForm); const subject = encodeURIComponent(data.get("subject") || "Ricky Kwok art inquiry"); const lines = [ `Name: ${data.get("name") || ""}`, `Email: ${data.get("email") || ""}`, `Inquiry type: ${data.get("type") || ""}`, `Artwork or series: ${data.get("artwork") || ""}`, "", data.get("message") || "" ]; const mailto = `mailto:studio@rickykwok.com?subject=${subject}&body=${encodeURIComponent(lines.join("\n"))}`; trackEvent("contact_form_submit", { inquiry_type: String(data.get("type") || "Unspecified"), has_artwork_context: data.get("artwork") ? "yes" : "no" }, () => { window.location.href = mailto; }); }); }
